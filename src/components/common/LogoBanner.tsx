'use client';
import React from 'react';

const LogoBanner = () => {
  return (
    <div className="inline-flex items-center px-4 py-1.5 rounded-full border border-border bg-background/60 backdrop-blur-xl">
      <span className="text-xs font-medium text-muted-foreground tracking-wide">
        Resume Tailor
      </span>
    </div>
  );
};

export default LogoBanner;
